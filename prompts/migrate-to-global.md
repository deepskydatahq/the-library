Check .claude/ for any local copies of skills, commands, experts, or hooks that are already installed globally in ~/.claude/. For each match, diff the local file against the global version. If they're identical or the global version is newer, delete the local copy. If the local file has project-specific customizations, keep it and show me the diff so I can decide. Don't touch any other files in .claude/.
